import { MessageEmbed } from "discord.js";
import { globalCommon } from "./global";

export default {
    message: globalCommon()
        .setTitle('Obrigado!')
        .setDescription('Obrigado por dar boost em nosso server, e como prometido, agora você tem comandos únicos para usar!')
}